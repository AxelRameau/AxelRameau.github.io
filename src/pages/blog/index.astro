---
import BaseHead from '../../components/BaseHead.astro';
import Header from '../../components/Header.astro';
import Footer from '../../components/Footer.astro';
import { SITE_TITLE, SITE_DESCRIPTION } from '../../consts';
import { getCollection } from 'astro:content';
import FormattedDate from '../../components/FormattedDate.astro';

const posts = (await getCollection('blog')).sort(
	(a, b) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf(),
);
---

<!doctype html>
<html lang="en">
	<head>
		<BaseHead title={SITE_TITLE} description={SITE_DESCRIPTION} />
		<!-- <style>
			ul li:nth-child(2){
				background-color: beige;
				color: var(--clr-noir);
				padding-top: 10rem;			}
			ul li:nth-child(4){
				background-color: beige;
				color: var(--clr-noir);
				padding-top: 10rem;	
			}
		</style> -->
	</head>
	<body class="bg-beige">
		<Header />
		<main class="flex flex-col gap-10 mt-36 mb-40">
				<ul class="flex flex-wrap items-center justify-center lg:flex-row gap-10 lg:gap-5">
					{
						posts.map((post) => (
							<li class="flex justify-center px-10 md:px-0 w-full md:w-[350px] lg:w-[500px] md:gap-10 md:py-5">
								<a class="flex flex-col gap-2.5 items-center  w-11/12" href={`/blog/${post.slug}/`}>
									<img class="w-full aspect-square object-cover" src={post.data.heroImage} alt="" />
									<h4 class="titre text-noir">{post.data.title}</h4>
								</a>
							</li>
						))
					}
				</ul>









					<!-- <ul class="flex flex-col items-center lg:flex-row gap-10 justify-center">
						<li class="flex flex-col gap-5 w-[400px] items-center">
							<img class="vignette_projet" src="img-portfolio/logo_mosbier.png" alt="logo du bar Mos&Bier">
							<h2 class="font-titre text-black text-2xl text-center"> Mos & Bier - bar</h2>
						</li>
						<li class="flex flex-col gap-5 w-[400px] items-center">
							<img class="vignette_projet" src="img-portfolio/logo_mosbier.png" alt="logo du bar Mos&Bier">
							<h2 class="font-titre text-black text-2xl text-center"> Mos & Bier - bar</h2>
						</li>
						<li class="flex flex-col gap-5 w-[400px] items-center">
							<img class="vignette_projet" src="img-portfolio/logo_mosbier.png" alt="logo du bar Mos&Bier">
							<h2 class="font-titre text-black text-2xl text-center"> Mos & Bier - bar</h2>
						</li>
					</ul> -->
			</main>
		<Footer />
	</body>
</html>
